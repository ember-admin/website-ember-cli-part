export default function(server) {
  server.createList('user', 3);
  server.createList('avatar', 3);
  server.createList('product', 3);
  server.createList('company', 3);
  server.createList('category', 3);
}
